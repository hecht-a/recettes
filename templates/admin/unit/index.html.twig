{% extends 'admin/card.html.twig' %}

{% block title %}
    Gestion des unités
{% endblock %}

{% block actions %}
    <a href="{{ path(prefix ~ '_new') }}" class="btn btn-primary">Ajouter une unité</a>
{% endblock %}

{% block content %}
    <table class="table">
        <thead>
        <tr>
            <th>id</th>
            <th>Label</th>
            <th>Label court</th>
            <th class="text-right">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for row in rows %}
            {% set link = path(prefix ~ '_edit', {id: row.id}) %}
            <tr>
                <td>
                    <a href="{{ link }}">{{ row.id }}</a>
                </td>
                <td>
                    <a href="{{ link }}">{{ row.label }}</a>
                </td>
                <td>
                    {{ row.shortLabel }}
                </td>
                <td>
                    <div class="dashboard-actions">
                        <a href="{{ link }}">
                            <svg class="icon icon-edit">
                                <use href="/icons.svg#edit"></use>
                            </svg>
                        </a>
                        <a href="{{ path(prefix ~ '_delete', {id: row.id}) }}">
                            <svg class="icon icon-delete">
                                <use href="/icons.svg#delete"></use>
                            </svg>
                        </a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ paginate(rows) }}
{% endblock %}
