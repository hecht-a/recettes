{% for type, messages in app.session.flashBag.all %}
    {% for message in messages %}
        <alert-message type="{{ type }}" title="{{ message.title }}" description="{{ message.description }}"></alert-message>
    {% endfor %}
{% endfor %}
