{% extends 'base.html.twig' %}

{% block title %}Liste de courses - {{ shoppingList.name }}{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('profile_index') }}" class="btn btn-secondary mb-4">← Retour à mon profil</a>

        <h1>{{ shoppingList.name }}</h1>
        <p class="text-muted">Créée le {{ shoppingList.createdAt|date('d/m/Y') }}</p>

        <h2 class="mt-4">🧾 Recettes incluses</h2>
        <ul class="mb-4">
            {% for recipe in shoppingList.getRecipes() %}
                <li>
                    {{ recipe.getRecipe().name }}
                </li>
            {% else %}
                <li>Aucune recette liée.</li>
            {% endfor %}
        </ul>

        <h2 class="mt-4">🛒 Ingrédients à acheter</h2>

        {% if shoppingList.ingredients is not empty %}
            <ul class="ingredient-list">
                {% for ingredient in shoppingList.ingredients %}
                    <li class="ingredient-line">
                        <label>
                            {{ ingredient.id }}
{#                            <input type="checkbox" {% if ingredient.isChecked %}checked{% endif %} disabled>#}
{#                            {{ ingredient.quantity|number_format(2, '.', ' ') }} {{ ingredient.ingredient.unit.shortLabel }}#}
{#                            — {{ ingredient.ingredient.name }}#}
                        </label>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>Cette liste ne contient aucun ingrédient.</p>
        {% endif %}
    </div>
{% endblock %}
